<!DOCTYPE html>
<html>
<head>
  <title>Songs</title>
</head>
<body>
  <h1>All Songs</h1>
  <ul>
    <% @songs.each do |song| %>
      <li>
        <a href="/songs/<%= song.id %>" style="text-decoration:none;"><%= song.name %>, by <%= song.artist %></a>
      </li>
    <% end %>
  </ul>
  <button type="button">
    <a href="/songs/new" style="text-decoration:none;">Add a new song!</a>
  </button>
  <button type="button">
    <a href="/genres/new" style="text-decoration:none;">Add a genre!</a>
  </button>
</body>
</html>